# Railway 배포 설정
# 이 파일을 backend/ 폴더 내에 두거나,
# Railway 대시보드에서 Root Directory를 backend/로 설정하세요.

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node src/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
